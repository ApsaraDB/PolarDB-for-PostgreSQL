import{_ as n,r as i,o as c,c as p,d as a,a as e,w as s,b as l,e as _}from"./app-e5d03054.js";const k="/PolarDB-for-PostgreSQL/assets/pr_parallel_execute_1-5ade2a03.png",g="/PolarDB-for-PostgreSQL/assets/pr_parallel_execute_2-f13fd3b5.png",h="/PolarDB-for-PostgreSQL/assets/pr_parallel_execute_task-6ddc37a4.png",u="/PolarDB-for-PostgreSQL/assets/pr_parallel_execute_dispatcher-ef5aa6cd.png",T="/PolarDB-for-PostgreSQL/assets/pr_parallel_execute_procs_1-37e52fe7.png",L="/PolarDB-for-PostgreSQL/assets/pr_parallel_execute_procs_2-777f9348.png",m="/PolarDB-for-PostgreSQL/assets/pr_parallel_execute_procs_3-c2b9a687.png",f="/PolarDB-for-PostgreSQL/assets/pr_parallel_replay_1-fa5b96d0.png",x="/PolarDB-for-PostgreSQL/assets/pr_parallel_replay_2-bf2d2654.png",N={},B=e("h1",{id:"wal-日志并行回放",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#wal-日志并行回放","aria-hidden":"true"},"#"),l(" WAL 日志并行回放")],-1),$={class:"table-of-contents"},A=_('<h2 id="背景" tabindex="-1"><a class="header-anchor" href="#背景" aria-hidden="true">#</a> 背景</h2><p>在 PolarDB for PostgreSQL 的一写多读架构下，只读节点（Replica 节点）运行过程中，LogIndex 后台回放进程（LogIndex Background Worker）和会话进程（Backend）分别使用 LogIndex 数据在不同的 Buffer 上回放 WAL 日志，本质上达到了一种并行回放 WAL 日志的效果。</p><p>鉴于 WAL 日志回放在 PolarDB 集群的高可用中起到至关重要的作用，将并行回放 WAL 日志的思想用到常规的日志回放路径上，是一种很好的优化思路。</p><p>并行回放 WAL 日志至少可以在以下三个场景下发挥优势:</p><ol><li>主库节点、只读节点以及备库节点崩溃恢复（Crash Recovery）的过程;</li><li>只读节点 LogIndex BGW 进程持续回放 WAL 日志的过程;</li><li>备库节点 Startup 进程持续回放 WAL 日志的过程。</li></ol><h2 id="术语" tabindex="-1"><a class="header-anchor" href="#术语" aria-hidden="true">#</a> 术语</h2><ul><li>Block：数据块</li><li>WAL：Write-Ahead Logging，预写日志</li><li>Task Node：并行执行框架中的子任务执行节点，可以接收并执行一个子任务</li><li>Task Tag：子任务的分类标识，同一类的子任务执行顺序有先后关系</li><li>Hold List：并行执行框架中，每个子进程调度执行回放子任务所使用的链表</li></ul><h2 id="原理" tabindex="-1"><a class="header-anchor" href="#原理" aria-hidden="true">#</a> 原理</h2><h3 id="概述" tabindex="-1"><a class="header-anchor" href="#概述" aria-hidden="true">#</a> 概述</h3><p>一条 WAL 日志可能修改多个数据块 Block，因此可以使用如下定义来表示 WAL 日志的回放过程：</p><ul><li>假设第 <code>i</code> 条 WAL 日志 LSN 为 $LSN_i$，其修改了 <code>m</code> 个数据块，则定义第 <code>i</code> 条 WAL 日志修改的数据块列表 $Block_i = [Block_{i,0}, Block_{i,1}, ..., Block_{i,m}]$；</li><li>定义最小的回放子任务为 $Task_{i,j}={LSN_i -&gt; Block_{i,j}}$，表示在数据块 $Block_{i,j}$ 上回放第 <code>i</code> 条 WAL 日志；</li><li>因此，一条修改了 <code>k</code> 个 Block 的 WAL 日志就可以表示成 <code>k</code> 个回放子任务的集合：$TASK_{i,*} = [Task_{i,0}, Task_{i,1}, ..., Task_{i,k}]$；</li><li>进而，多条 WAL 日志就可以表示成一系列回放子任务的集合：$TASK_{<em>,</em>} = [Task_{0,<em>}, Task_{1,</em>}, ..., Task_{N,*}]$；</li></ul><p>在日志回放子任务集合 $Task_{<em>,</em>}$ 中，每个子任务的执行，有时并不依赖于前序子任务的执行结果。假设回放子任务集合如下：$TASK_{<em>,</em>} = [Task_{0,<em>}, Task_{1,</em>}, Task_{2,*}]$，其中：</p><ul><li>$Task_{0,*}=[Task_{0,0}, Task_{0,1}, Task_{0,2}]$</li><li>$Task_{1,*}=[Task_{1,0}, Task_{1,1}]$，</li><li>$Task_{2,*}=[Task_{2,0}]$</li></ul><p>并且 $Block_{0,0} = Block_{1,0}$，$Block_{0,1} = Block_{1,1}$，$Block_{0,2} = Block_{2,0}$</p><p>则可以并行回放的子任务集合有三个：$[Task_{0,0},Task_{1,0}]$、$[Task_{0,1},Task_{1,1}]$、$[Task_{0,2},Task_{2,0}]$</p><p>综上所述，在整个 WAL 日志所表示的回放子任务集合中，存在很多子任务序列可以并行执行，而且不会影响最终回放结果的一致性。PolarDB 借助这种思想，提出了一种并行任务执行框架，并成功运用到了 WAL 日志回放的过程中。</p><h3 id="并行任务执行框架" tabindex="-1"><a class="header-anchor" href="#并行任务执行框架" aria-hidden="true">#</a> 并行任务执行框架</h3><p>将一段共享内存根据并发进程数目进行等分，每一段作为一个环形队列，分配给一个进程。通过配置参数设定每个环形队列的深度：</p><p><img src="'+k+'" alt="image.png"></p><ul><li>Dispatcher 进程 <ul><li>通过将任务分发给指定的进程来控制并发调度；</li><li>负责将进程执行完的任务从队列中删除；</li></ul></li><li>进程组 <ul><li>组内每一个进程从相应的环形队列中获取需要执行的任务，根据任务的状态决定是否执行。</li></ul></li></ul><p><img src="'+g+'" alt="image.png"></p><h4 id="任务" tabindex="-1"><a class="header-anchor" href="#任务" aria-hidden="true">#</a> 任务</h4><p>环形队列的内容由 Task Node 组成，每个 Task Node 包含五个状态：Idle、Running、Hold、Finished、Removed。</p><ul><li><code>Idle</code>：表示该 Task Node 未分配任务；</li><li><code>Running</code>：表示该 Task Node 已经分配任务，正在等待进程执行，或已经在执行；</li><li><code>Hold</code>：表示该 Task Node 有前向依赖的任务，需要等待依赖的任务执行完再执行；</li><li><code>Finished</code>：表示进程组中的进程已经执行完该任务；</li><li><code>Removed</code>：当 Dispatcher 进程发现一个任务的状态已经为 <code>Finished</code>，那么该任务所有的前置依赖任务也都应该为 <code>Finished</code> 状态，<code>Removed</code> 状态表示 Dispatcher 进程已经将该任务以及该任务所有前置任务都从管理结构体中删除；可以通过该机制保证 Dispatcher 进程按顺序处理有依赖关系的任务执行结果。</li></ul><p><img src="'+h+'" alt="image.png"></p><p>上述状态机的状态转移过程中，黑色线标识的状态转移过程在 Dispatcher 进程中完成，橙色线标识的状态转移过程在并行回放进程组中完成。</p><h4 id="dispatcher-进程" tabindex="-1"><a class="header-anchor" href="#dispatcher-进程" aria-hidden="true">#</a> Dispatcher 进程</h4><p>Dispatcher 进程有三个关键数据结构：Task HashMap、Task Running Queue 以及 Task Idle Nodes。</p><ul><li><strong>Task HashMap</strong> 负责记录 Task Tag 和相应的执行任务列表的 hash 映射关系： <ul><li>每个任务有一个指定的 Task Tag，如果两个任务间存在依赖关系，则它们的 Task Tag 相同；</li><li>在分发任务时，如果一个 Task Node 存在前置依赖任务，则状态标识为 <code>Hold</code>，需等待前置任务先执行。</li></ul></li><li><strong>Task Running Queue</strong> 负责记录当前正在执行的任务；</li><li><strong>Task Idel Nodes</strong> 负责记录进程组中不同进程，当前处于 <code>Idle</code> 状态的 Task Node；</li></ul><p>Dispatcher 调度策略如下：</p><ul><li>如果要执行的 Task Node 有相同 Task Tag 的任务在执行，则优先将该 Task Node 分配到该 Task Tag 链表最后一个 Task Node 所在的执行进程；目的是让有依赖关系的任务尽量被同一个进程执行，减少进程间同步的开销；</li><li>如果期望优先分配的进程队列已满，或者没有相同的 Task Tag 在执行，则在进程组中按顺序选择一个进程，从中获取状态为 <code>Idle</code> 的 Task Node 来调度任务执行；目的是让任务尽量平均分配到不同的进程进行执行。</li></ul><p><img src="'+u+'" alt="image.png"></p><h4 id="进程组" tabindex="-1"><a class="header-anchor" href="#进程组" aria-hidden="true">#</a> 进程组</h4><p>该并行执行针对的是相同类型的任务，它们具有相同的 Task Node 数据结构；在进程组初始化时配置 <code>SchedContext</code>，指定负责执行具体任务的函数指针：</p><ul><li><code>TaskStartup</code> 表示进程执行任务前需要进行的初始化动作</li><li><code>TaskHandler</code> 根据传入的 Task Node，负责执行具体的任务</li><li><code>TaskCleanup</code> 表示执行进程退出前需要执行的回收动作</li></ul><p><img src="'+T+'" alt="image.png"></p><p>进程组中的进程从环形队列中获取一个 Task Node，如果 Task Node 当前的状态是 <code>Hold</code>，则将该 Task Node 插入到 Hold List 的尾部；如果 Task Node 的状态为 Running，则调用 TaskHandler 执行；如果 TaskHandler 执行失败，则设置该 Task Node 重新执行需要等待调用的次数，默认为 3，将该 Task Node 插入到 Hold List 的头部。</p><p><img src="'+L+'" alt="image.png"></p><p>进程优先从 Hold List 头部搜索，获取可执行的 Task；如果 Task 状态为 Running，且等待调用次数为 0，则执行该 Task；如果 Task 状态为 Running，但等待调用次数大于 0，则将等待调用次数减去 1。</p><p><img src="'+m+'" alt="image.png"></p><h3 id="wal-日志并行回放-1" tabindex="-1"><a class="header-anchor" href="#wal-日志并行回放-1" aria-hidden="true">#</a> WAL 日志并行回放</h3><p>根据 LogIndex 章节介绍，LogIndex 数据中记录了 WAL 日志和其修改的数据块之间的对应关系，而且 LogIndex 数据支持使用 LSN 进行检索，鉴于此，PolarDB 数据库在 Standby 节点持续回放 WAL 日志过程中，引入了上述并行任务执行框架，并结合 LogIndex 数据将 WAL 日志的回放任务并行化，提高了 Standby 节点数据同步的速度。</p><h4 id="工作流程" tabindex="-1"><a class="header-anchor" href="#工作流程" aria-hidden="true">#</a> 工作流程</h4><ul><li>Startup 进程：解析 WAL 日志后，仅构建 LogIndex 数据而不真正回放 WAL 日志；</li><li>LogIndex BGW 后台回放进程：成为上述并行任务执行框架的 Dispatcher 进程，利用 LSN 来检索 LogIndex 数据，构建日志回放的子任务，并分配给并行回放进程组；</li><li>并行回放进程组内的进程：执行日志回放子任务，对数据块执行单个日志的回放操作；</li><li>Backend 进程：主动读取数据块时，根据 PageTag 来检索 LogIndex 数据，获得修改该数据块的 LSN 日志链表，对数据块执行完整日志链的回放操作。</li></ul><p><img src="'+f+'" alt="image.png"></p><ul><li>Dispatcher 进程利用 LSN 来检索 LogIndex 数据，按 LogIndex 插入顺序枚举 PageTag 和对应 LSN，构建{LSN -&gt; PageTag}，组成相应的 Task Node；</li><li>PageTag 作为 Task Node 的 Task Tag；</li><li>将枚举组成的 Task Node 分发给并行执行框架中进程组的子进程进行回放；</li></ul><p><img src="'+x+`" alt="image.png"></p><h2 id="使用方法" tabindex="-1"><a class="header-anchor" href="#使用方法" aria-hidden="true">#</a> 使用方法</h2><p>在 Standby 节点的 <code>postgresql.conf</code> 中添加以下参数开启功能：</p><div class="language-ini" data-ext="ini"><pre class="language-ini"><code><span class="token key attr-name">polar_enable_parallel_replay_standby_mode</span> <span class="token punctuation">=</span> <span class="token value attr-value">ON</span>
</code></pre></div>`,50);function S(d,W){const r=i("Badge"),t=i("ArticleInfo"),o=i("router-link");return c(),p("div",null,[B,a(r,{type:"tip",text:"V11 / v1.1.17-",vertical:"top"}),a(t,{frontmatter:d.$frontmatter},null,8,["frontmatter"]),e("nav",$,[e("ul",null,[e("li",null,[a(o,{to:"#背景"},{default:s(()=>[l("背景")]),_:1})]),e("li",null,[a(o,{to:"#术语"},{default:s(()=>[l("术语")]),_:1})]),e("li",null,[a(o,{to:"#原理"},{default:s(()=>[l("原理")]),_:1}),e("ul",null,[e("li",null,[a(o,{to:"#概述"},{default:s(()=>[l("概述")]),_:1})]),e("li",null,[a(o,{to:"#并行任务执行框架"},{default:s(()=>[l("并行任务执行框架")]),_:1})]),e("li",null,[a(o,{to:"#wal-日志并行回放-1"},{default:s(()=>[l("WAL 日志并行回放")]),_:1})])])]),e("li",null,[a(o,{to:"#使用方法"},{default:s(()=>[l("使用方法")]),_:1})])])]),A])}const b=n(N,[["render",S],["__file","avail-parallel-replay.html.vue"]]);export{b as default};
